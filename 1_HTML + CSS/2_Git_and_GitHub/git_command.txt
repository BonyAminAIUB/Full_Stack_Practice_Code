git config --global user.email "mdbonyamin2002@gmail.com"
git config --global user.name "BonyAminAIUB"
git config --global --list    --> it shows my email and username

git init        --> one time each project to start git
git add .       --> every time to add
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/BonyAminAIUB/Full_Stack_Practice_Code.git
git push -u origin main

after change in my local
then

git add .
git commit -m "any message"
git push


some advance command

git clone https://github.com/BonyAminAIUB/Full_Stack_Practice_Code.git
git checkout main  --> branch change to main
git branch   --> show all the branch 
git checkout -b branch_name   --> create new branch 
git push --set-upstream origin branch_name   --> push file to a new file 
git pull   --> change file download to my local repo