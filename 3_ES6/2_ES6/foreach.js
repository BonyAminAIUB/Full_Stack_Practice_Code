// forEach((element, index, array) => { 
//    //code
//    });

let numbers = [10, 20, 30, 40, 50];
// forEach function --> to modify array and get new array
numbers.forEach((elem, ind, arr) => {
    console.log(elem, ind, arr);
});
    
